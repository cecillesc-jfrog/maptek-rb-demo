items.find({
  "@rb.project":     {"$eq": "maptek-geo-suite"},
  "@rb.pipeline":    {"$eq": "github-actions"},
  "@component.team": {"$eq": "platform"},
  "repo":            {"$match": "maptek-docker-local"}
}).include(
  "name", "path", "repo", "created", "property"
).sort({"$asc": ["path"]})
