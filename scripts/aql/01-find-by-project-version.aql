items.find({
  "@project.name":    {"$eq": "maptek-geo-suite"},
  "@project.version": {"$eq": "2026.demo.2"},
  "repo":             {"$match": "maptek-docker-local"}
}).include(
  "name", "repo", "path", "created", "size"
).sort({"$asc": ["path"]})
