items.find({
  "@rb.project":    {"$eq": "maptek-geo-suite"},
  "@rb.build_type": {"$eq": "release"},
  "repo":           {"$match": "maptek-docker-local"}
}).include(
  "name", "path", "repo", "created"
).sort({"$desc": ["created"]})
